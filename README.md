# FastAPI URL Shortener

## Deveop

### Setup:

Right click "url-shortener" -> Mark directory as -> Sources Root

### Run

Go to workdir:
```shell
cd url-shortener
```

Run dev server:
```shell
fastapi dev
```