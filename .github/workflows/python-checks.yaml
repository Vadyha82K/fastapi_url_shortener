name: Python checks 🐍

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder GH Action step
        run: echo "Hello action!"
